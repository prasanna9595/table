import { OtTooltipDirective } from './ot-tooltip.directive';

describe('OtTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new OtTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
