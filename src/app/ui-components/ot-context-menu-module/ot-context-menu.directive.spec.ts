import { OtContextMenuDirective } from './ot-context-menu.directive';

describe('OtContextMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new OtContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
